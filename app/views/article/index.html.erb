<% @articles.each do |article| %>
  <%= link_to article_path(article) do %>
    <h2><%= article.name %></h2>
  <% end %>
  <ul>
    <li><%= article.content %>
  </ul>
<% end %>

<%= link_to "New article", new_article_path(@article) %>
